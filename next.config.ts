import { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,  // Ensures React strict mode is enabled
  // Remove `swcMinify` as it's no longer supported
};

export default nextConfig;
